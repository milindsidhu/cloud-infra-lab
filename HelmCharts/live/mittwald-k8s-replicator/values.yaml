kubernetes-replicator:
  resources:
    requests:
      cpu: "250m"
      memory: "256Mi"
    limits:
      cpu: "500m"
      memory: "512Mi"
  podLabels:
    app: mittwald-k8s-replicator
    env: dev