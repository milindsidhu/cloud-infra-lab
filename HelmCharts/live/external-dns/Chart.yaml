apiVersion: v2
name: external-dns
description: A Helm chart for external-dns
type: application
version: 0.1.0
dependencies:
  - name: external-dns
    version: 8.9.2     # Bitnami chart version
    repository: https://charts.bitnami.com/bitnami
    condition: external-dns.enabled
    tags:
      - external-dns
